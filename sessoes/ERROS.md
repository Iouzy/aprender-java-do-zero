# Erros do compilador — registo corrido

Append-only. Nunca se reescreve, nunca se resume: é o histórico clínico.
Escrito pela Routine noturna a partir de `sessoes/erros-bruto.log`, que o `jc`
enche sozinho a cada compilação.

Para saber onde atacar: `grep -i "buffer" sessoes/ERROS.md` diz quantas vezes
a mesma fraqueza já te apanhou, e quando foi a última.

| Data | Exercício | Versão | Erro do javac (em bruto) | Fraqueza |
|---|---|---|---|---|
